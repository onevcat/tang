# tang

`tang` is a work-in-progress command-line client for Tangled.

The project is not yet usable for day-to-day workflows. The initial milestones build toward auth, repository, issue, pull request, SSH key, status, config, browse, and completion commands.

## Development

```sh
make test
make build
./bin/tang --help
```
